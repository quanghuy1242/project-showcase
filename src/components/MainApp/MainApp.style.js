import { mergeStyleSets } from '@uifabric/styling';

export const style = mergeStyleSets({
  app: {
    display: 'flex',
    flexDirection: 'column'
  },
  appMain: {
    display: "flex"
  }
})